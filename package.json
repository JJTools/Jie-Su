{
  "name": "jie-su-magic-book",
  "version": "1.0.0",
  "private": true,
  "description": "Static magic book page deployable on Vercel.",
  "scripts": {
    "build": "node -e \"console.log('Static site: no build step required')\""
  }
}
